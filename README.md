# django-beginner-project-step-by-step
pip install anaconda
pip install keras
pip install tensorflow
pip install django


django-admin startproject projectname
cd projectname
python manage.py runserver
# Coming Soon
